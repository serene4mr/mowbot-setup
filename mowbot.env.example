# Copy to mowbot.env (or run scripts/install.sh, which creates mowbot.env).
# Same file as systemd ExecStart: docker compose --env-file mowbot.env ...

MB_ROBOT_ID=
MB_MANUFACTURER=
MB_ROBOT_MODEL=
MB_SENSOR_MODEL=

MB_MQTT_HOST=localhost
MB_MQTT_PORT=1883
MB_MQTT_USE_TLS=false
MB_MQTT_USER=
MB_MQTT_PASSWORD=
